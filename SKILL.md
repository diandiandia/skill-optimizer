---
name: skill-optimizer
description: 专门优化 reachable-critical-audit skill 的复盘-修复闭环 skill：四缺陷评估纪律（过设计/设计偏见/死代码/盲目带入）→ 取证先行 → C0 设计五件套 → 分层开发 → 版本链+测试验收 → 安装提交 → 验收审计。当用户要求评估、修复、优化 reachable-critical-audit 时使用。
---

# Skill Optimizer — reachable-critical-audit 优化闭环

## 环境定位（禁硬编码绝对路径——纪律 #8 对本 skill 自身同样适用）

一切路径从 `$HOME` 探测，开跑前先执行：
```bash
DEV_REPO=$(find "$HOME" -maxdepth 3 -type d -name "reachable-critical-audit-v3" | head -1)
SKILL_DIR="$HOME/.claude/skills/reachable-critical-audit"
MEM_DIR=$(find "$HOME/.claude/projects" -maxdepth 2 -type d -name memory | head -1)
# 审计项目根 := $HOME（本环境惯例 $HOME/<project>; 项目在别处时以
# 会话上下文/记忆索引中的实际路径为准, 不得臆造）
```
本 skill 自身同样受它编码的纪律约束：**每条规则都必须带案例支撑**
（v3.12-v3.16 周期的会话内实录），零凭记忆转述。

## 闭环总览

```
阶段 0 复盘评估（用户问四问）
  → 阶段 1 取证（缺陷逐条代码核实, 已存在机制裁除）
  → 阶段 2 C0 设计五件套（REQ/SWR/SYSTEM/SOFTWARE_DESIGN/BIAS_EVAL）
  → 阶段 3 分层开发（P1 机械 → P2 结构 → P3 内容 → P4 版本链）
  → 阶段 4 测试验收（test_v3XX + 全量回归 + 旧队列零新增告警）
  → 阶段 5 安装提交（install.sh + conventional commits）
  → 阶段 6 验收审计（未审计新项目闭环）→ 回到阶段 0
```

## 阶段 0：复盘评估

**触发**：审计批次收官后用户提出四问（能力评估/暴露问题/修复方案/四缺陷）。

**必读 lesson 义务（先读后评估，禁止跳过）**——统一读入口为**各项目
`.audit_results/lessons.md`**（v3.16.1 用户裁定：reachable-critical-audit 在
审计收官时把 lessons 写到项目本地 .audit_results；本 skill 从这里读）：
1. 按机械清单逐项确认并 Read：对每个已审计项目目录执行
   `ls <项目>/.audit_results/lessons.md`，存在即读全文
   （`ls "$HOME"/*/.audit_results/lessons.md` 一次枚举全集）；
2. 记忆索引 `skill-v3XX-defect-evaluation.md` 与各项目审计结论记忆
   （`$MEM_DIR/`，环境定位段探测）——补充各周期评估上下文；
3. 目标仓 `$DEV_REPO/lessons/` 仅作战役期历史追溯（W5/W6 档案），
   不是新教训的读入口。
读完才允许写缺陷清单——每条缺陷的「案例支撑来源」必须指向某项目
lessons.md 的具体条目（"某项目 lessons.md 第 N 条"），无条目的候选缺陷
**不得入库**。

**输入**：上述必读清单 + 六门禁输出 + 队列/反驳波实录。

**输出**：缺陷清单。每条缺陷必须含：
- **案例支撑来源**：具体到会话内产物（某项目 lessons.md 第 N 条/某次门禁
  violations 输出/某候选 refutation 原文）——禁止「凭印象/凭历史记忆」；
- **初判分层**：P1 机械（可复现代码缺陷）/ P2 结构（已热修复缺测试守卫/
  契约未化）/ P3 内容（任务书义务/清单条目）/ P4 流程条款。

**能力评估判据**（四问第一问，用批次实录回答）：
- 文件解析：R2 drop 经 R4 复核逐点核实的比例（防御确认能力）；
- 协议分析：反驳轮产出协议语义修正的深度（树外契约/大小写变体类）；
- 状态机分析：状态机族机制是否真实拦截（verifier 死代码误判被复活者
  逐转移行推翻为最高价值形态）。

**四缺陷评估**（贯穿设计+实现，见阶段 2/7）——这是验收硬性项。

## 阶段 1：取证（设计前强制）

1. 逐条缺陷读 dev 树代码，确认**编辑点行号**与**当前状态**（是否已被
   热修复/已存在机制）。案例：v3.16 原拟 D-6（collect 边缺口）取证发现
   SWR-V3.10-006 已在位 → 裁除（过设计防线的标准动作）。
2. 检查资产面：清单/先例/模板/注入文本的**去项目化状态**（阶段 7）。
3. 取证结论写入设计件的「代码核实」列——设计件里的每个行号都必须是在
   取证时实测过的，禁止凭记忆写行号。

## 阶段 2：C0 设计五件套

落盘 `docs/design/`：
- `REQ_V3_XX.md`：缺陷清单表（#/缺陷（代码核实）/修复/编辑点）+ 测试守卫
  约束 + 开发序列 + 验证命令；
- `SWR_V3_XX.md`：每条修复一条 SWR，含裁决理由 + 测试守卫；
- `SYSTEM_DESIGN_V3_XX.md`：变更边界不变式（**不改阶段骨架、六门禁判据
  语义、队列数据模型主体**）+ 模块影响面 + 兼容性；
- `SOFTWARE_DESIGN_V3_XX.md`：函数级设计 + P 分层序列；
- `BIAS_EVAL_V3_XX.md`：四缺陷评估（阶段 7 的细则）。

**义务入库三问**（v3.3.2 纪律，设计件每项新义务必过）：
① 触发条件是什么（无条件默认不建）；② 消费者是谁（无消费者不建，或先建
消费者）；③ 裁掉丢什么（无案例支撑的防御性义务降为 checklist 提示）。

**修法形态纪律**：
- 建议映射/warn/提示级优先——**禁止自动改写**（误猜风险>收益先例：
  v3.14 D-4 裁自动 re-credit、D-3 建议映射不自动归一化）；
- 已有机制的缺陷禁止重造（阶段 1 裁除条款）。

## 阶段 3：分层开发

- **P1 机械小修**：判定函数一致化/守卫条件/告警结构化——单点代码缺陷；
- **P2 结构修复**：schema 契约化 + **热修复补测试守卫**（v3.15 教训：
  热修复不配测试是流程偏见，正式化必须带测试）；
- **P3 内容**：任务书义务/清单条目/模板条款——全部提示级；
- **P4 版本链五件**：
  1. `workflow_export.py:TOOLING_VERSION` 前进；
  2. 版本守卫测试行更新（test_v310/v312/v313/v39/v314/v315 等含版本号
     断言处，逐处 sed 并核对）；
  3. SKILL.md 增量段（列 SWR 号 + 验收判据）；
  4. `REQUIREMENTS_TRACKING.md` 手工追加段（**禁止运行 gen_tracking
     再生成**）+ gen_tracking VERSIONS 登记；
  5. 资产计数守卫同步（先例/清单条数在 SKILL.md 附录与 R3/R3.5 正文节
     多处出现——test_doc_lint 只守附录行，正文节也要改，v3.15 实录）。

## 阶段 4：测试验收

- 新增 `tests/test_v3XX.py`：每个 SWR 至少一用例（含反面分支——如
  建议不自动改写/无约束零输出）；
- 全量回归全绿；
- **旧队列复跑零新增告警**：代表项目队列（gpac/freetype/av 等）跑
  assert_ledger，violations 中 blocking 必须为 0，warn 必须与变更前一致；
- `signature_lib.py selfcheck <非 fixture 项目>` 资产通用性不回退。

## 阶段 5：安装提交

1. `bash install.sh`（自带全量测试）→ installed 副本验证新机制在位
   （TOOLING/关键函数 hasattr）；
2. `diff` dev/installed 文件列表一致（排除 pycache）；
3. conventional commits 分组：
   - `vX.YY C0: 设计五件套 ...`
   - `vX.YY P1-Px: 实现 ...`
   - `vX.YY Px: 版本链 ...`
   （v3.15/v3.16 实测形态，一个周期 1-3 个 commit）。

## 阶段 6：验收审计（闭环）

每版本需**未审计过的新项目**做验收（REQ-V3.4-008：优先补覆盖账本缺口格）。
验收审计约束如实预对齐（零 clone/无构建环境 → 实证类 claim 降
NEEDS_REVIEW 不伪造实证，v3.15 av 验收实录）。验收审计的 lessons 又成为
下一轮阶段 0 的输入——闭环。

## 阶段 7：四缺陷评估细则（验收硬性项）

设计期写 BIAS_EVAL（阶段 2），**实现期必须重跑一遍**（v3.16 教训：设计件
写着零违规，实现时仍会把案例名写进正文——实现期验收抓出 15 处）。

**① 盲目带入历史审计信息**：检查点——
- 每条缺陷的案例支撑是否为会话内实录（阶段 0 必读 lesson 义务——
  **评估与开发全程随时回读**对应 lessons 条目核对，不得凭开读一次的记忆
  转述；设计件写完后再对照 lessons 原文核对一遍案例数字/措辞）；
- **去项目化**：运行时正文（清单 steps/先例五字段/任务书模板/注入 prompt
  文本）零项目名/零框架名——归属只允许进 `source_lessons`/追溯字段；
  机器守卫：`tests/test_deproject_assets.py` 的 PROJECT_TOKENS 扫描
  （v3.16 扩展三条新断言防回退——新增资产正文若带项目名必须同步扩守卫）。

**② 设计偏见**：检查点——
- 是否把自身编排便利当 skill 义务（v3.14 D-7 裁除实录）；
- 是否自动改写（禁止，见阶段 2）；
- 修法是否落在正确层级（agent 行为偏差 ≠ skill 硬义务——容忍契约优先，
  v3.15 D-6/D-8 实录）。

**③ 死代码**：检查点——
- 每个新函数/新字段列出消费者（无消费者不建）；
- 附带裁决：疑似死参数先查消费者再裁（v3.15 20% 抽样保留裁决）；
- 开发时顺手清已证实死函数（v3.15 `_detect_languages` 清理实录）。

**④ 过设计**：检查点——
- 无新门禁名、无新强制义务、无新阶段（v3.15/v3.16 全周期守住）；
- 义务三问逐项过（阶段 2）；
- 取证裁除已存在机制（阶段 1）。

## 核心纪律速查（十条）

1. **必读 lesson**：评估/设计/验收前按机械清单 Read（唯一读入口=各项目
   `.audit_results/lessons.md`，`ls "$HOME"/*/.audit_results/lessons.md`
   一次枚举 + $MEM_DIR 记忆索引补充），案例支撑指向具体条目——
   零凭记忆转述；
2. 取证先行：设计件行号全部实测，已存在机制裁除；
3. 义务入库三问：触发条件/消费者/案例支撑；
4. 不自动改写：建议映射/warn/提示级，误猜风险>收益；
5. 热修复必须配测试：正式化即带守卫；
6. 版本链五件 + 禁 gen_tracking 再生成；
7. 旧队列复跑零新增告警（兼容性不变式）；
8. 第一原则三禁止：项目名/机器路径禁入运行时正文与运行时逻辑（sibling
   推导相对形态，v3.16 D-5 实录；本 skill 自身 2026-08-30 曾硬编码绝对路径被用户当场纠正——环境定位一律 $HOME 探测，本案即案例）；
9. 双副本同步：dev == installed + install.sh 官方路径；
10. 实现期四缺陷重跑 + 机器守卫防回退（deproject 扫描扩展随违规同步扩）。

## 恢复点

- 设计件：`docs/design/*_V3_XX.md` 五件套；
- 状态：`docs/design/REQUIREMENTS_TRACKING.md` 手工段（各版本最后一段）；
- 闭环入口：各项目 `.audit_results/lessons.md`（唯一读入口, 写侧=
  reachable-critical-audit 收官落盘）+ 记忆索引
  `skill-v3XX-defect-evaluation.md`（每周期一份，含缺陷清单与四缺陷结论）。
